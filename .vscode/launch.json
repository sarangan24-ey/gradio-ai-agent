{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Gradio Agent",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/src/agent_gradio.py",
      "envFile": "${workspaceFolder}/.env",
      "console": "integratedTerminal"
    }
  ]
}
